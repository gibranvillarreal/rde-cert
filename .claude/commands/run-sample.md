Run the backlog assistant against the first sample input:

```
python main.py --notes samples/meeting1.txt --backlog samples/backlog.json --verbose
```

Show me the generated user stories and point out any issues with the output quality.
