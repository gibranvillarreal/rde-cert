Run the backlog assistant against all three sample inputs and report results:

1. `python main.py --notes samples/meeting1.txt --backlog samples/backlog.json`
2. `python main.py --notes samples/meeting2.txt --backlog samples/backlog.json`
3. `python main.py --notes samples/meeting3.pdf --backlog samples/backlog.json`

For each run, report: number of stories generated, duplicates detected, any errors.
Flag any output that looks low quality or unexpected.
